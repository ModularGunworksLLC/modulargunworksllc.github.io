# Order data (stored locally when using serve.js; do not commit)
data/orders.json

# Environment and secrets (never commit)
.env
.env.*
!.env.example

# Local admin tooling (not part of main/live site)
admin/
